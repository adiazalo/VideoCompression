Test.m file reads all frames from .mp4 clip 
you may also wish to store specific frame in a variable in which 
you can pass them to another function. 
The count acts as frame number inside the while loop 